services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      PYTHON_VERSION: 3.11.9
